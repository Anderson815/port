model SkillEntity {
  id          Int     @id @default(autoincrement())
  name        String  @db.VarChar(50)
  description String  @db.VarChar(500)
  imageUrl    String? @map("image_url") @db.VarChar(350)

  @@map("skill")
}
